import { NavigationContainer } from '@react-navigation/native'
import { GetStartedNavigation } from './GetStarted.navigation'

export const Navigation = () => {
    return <NavigationContainer>
        <GetStartedNavigation />
    </NavigationContainer>
}